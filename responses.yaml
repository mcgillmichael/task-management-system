components:
  responses:
    HealthCheckResponse:
      description: Health status of the application.
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: string
                example: "available"
              environment:
                type: string
                example: "development"
              version:
                type: string
                example: "1.0.0"